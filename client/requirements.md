## Packages
framer-motion | Page transitions and scroll-triggered animations
react-hook-form | Form state management
@hookform/resolvers | Zod validation for forms
clsx | Utility for constructing className strings
tailwind-merge | Utility for merging tailwind classes safely

## Notes
- Smooth scrolling requires CSS `scroll-behavior: smooth` on the html element
- Ensure the backend seeds the database with some projects and achievements so the UI looks complete on first load
- Dark mode is enabled by default via the 'dark' class on the body/html
